const initialState = {
    results: null,
};

const reducer = function(state = initialState, action = {}) {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;
